---
import '@fontsource/inter/variable.css';

// Nunito
// Dosis
---

<style is:inline is:global>
  :root {
    --aw-font-sans: 'InterVariable';
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: var(--aw-font-sans);

    --aw-color-primary: #424c55;
    --aw-color-secondary: #5d4e60;
    --aw-color-accent: #70a9a1;
    --aw-color-text-page: #031926;
    --aw-color-text-muted: #03191e;
    --aw-color-bg-page: #fcfaf8;
  }
</style>
